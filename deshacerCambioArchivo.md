## Revertir cambios en un repositorio hasta el último commit

```sh 
$ git checkout -- .
```

## Revertir cambios en el archivo indicado hasta el último commit

```sh 
$ git checkout -– nombreArchivo
```